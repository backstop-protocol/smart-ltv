{
  "name": "morpho-allocator",
  "version": "1.0.0",
  "description": "**Foundry is a blazing fast, portable and modular toolkit for Ethereum application development written in Rust.**",
  "main": "soljson-v0.8.23+commit.f704f362.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "forge test",
    "test-goerli": "forge test --match-path './test/integration/**' --fork-url $GOERLI_RPC_URL -vvv",
    "build": "forge build",
    "prettier-format": "prettier --config .prettierrc \"./(src|test)/**/*.sol\" --write",
    "prettier-watch": "onchange \"(src|test)/**/*.sol\" -- prettier --write {{changed}}"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.1.0",
    "prettier-plugin-solidity": "^1.2.0",
    "onchange": "^7.1.0"
  }
}